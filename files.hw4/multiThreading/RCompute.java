package hw4.p1; 

public class RCompute implements runnable {
int left = 40
int right = 250
    public RCompute averageOfSquares(left, right){
        double sum = right * (right +1)/2 - (left -1) * left / 2;
        double counter = right - left + 1
        
        average = sqrt(sum) / counter; 
    public void run(){
        
    }
}
